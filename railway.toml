# Восстановлен после повреждения при загрузке через GitHub UI.
# Актуальный деплой -- РФ-хостинг (Timeweb Cloud), файл оставлен для
# совместимости с Railway-style запуском.
[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
