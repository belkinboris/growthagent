"""
Общий контракт для всех коннекторов (project metrics api, metrika, direct,
yookassa). Не обязателен к наследованию в v1 -- каждый коннектор в v1 это
просто набор async-функций (см. truepost.py), а не класс. Этот файл
описывает соглашение по сигнатурам и поведению при ошибках, чтобы
коннекторы оставались взаимозаменяемыми по форме результата.

Соглашение:
- любая ошибка сети/авторизации/формата бросается как исключение,
  специфичное для коннектора (например TruePostConnectorError);
- вызывающий код (scheduler.py) перехватывает её и помечает
  Integration.status = error, Integration.last_error = str(exc);
- успешный результат -- это dict с нормализованными ключами
  (см. config.CORE_FUNNEL_KEYS) либо метриками источника (для metrika/direct
  это будут traffic-метрики: spend, clicks, ctr, cpc -- не воронка продукта);
- поле "as_of" обязательно там, где источник способен его предоставить.
"""
