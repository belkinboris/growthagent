# Дорожная карта: из внутреннего инструмента в коммерческий продукт

Этот файл — источник правды для непрерывной работы над платформой.
Почасовая рутина читает его, берёт **одну** задачу, делает её целиком
и дописывает результат в журнал внизу.

## Что мы строим

«Аналитик Воронки» — платформа, которая подключается к любому digital-продукту
по HTTP и отвечает владельцу на три вопроса: **что происходит**, **где теряются
люди**, **что делать дальше**. Первый подключённый продукт — АвтоПост
(projectautopost.ru), он же живой полигон. Цель — продукт, который не стыдно
продавать: понятный человеку, который видит его впервые.

Экосистема: «Создатель» ведёт проект на ранних стадиях (идея → лендинг →
проверка спроса), Аналитик подхватывает, когда продукт живой. Платформы
могут слиться или остаться отдельными — архитектура позволяет оба пути
(вся платформа живёт под префиксом `/growth`).

## Принципы, которые нельзя нарушать

Они важнее скорости и важнее красоты. Если задача противоречит принципу —
задача неверна, а не принцип.

1. **Честность данных.** Никогда не показывать выдуманное или экстраполированное
   число как факт. Нет данных — так и писать. Малая выборка — говорить об этом
   словами, а не рисовать уверенный график.
2. **Одна проверка за раз.** Growth Loop меняет одну переменную, фиксирует
   базовый уровень, ждёт выборку, выносит вердикт. Функции, которые позволяют
   менять всё сразу, ломают продукт по смыслу.
3. **Аналитик не принимает решений за владельца.** Он объясняет и предлагает;
   кнопку нажимает человек.
4. **Приватность.** Наружу отдаются агрегаты и анонимные ключи. Ни email,
   ни id пользователей продукта в аналитике быть не должно.
5. **Ядро не знает продуктовой специфики.** Всё, что специфично для АвтоПоста,
   живёт в коннекторах и настройках проекта. Новый клиент подключается без
   правок кода.
6. **Работает из России.** LLM — Yandex (YandexGPT / DeepSeek). Telegram —
   через релей. Никаких зависимостей, недоступных с РФ-хостинга.

## Бэклог

Приоритет сверху вниз. Внутри блока — тоже по убыванию пользы.
Взял задачу — отметь `[~]`, закончил — `[x]` и строка в журнале.

### A. Понятность (главное для продажи)

- [ ] A1. Пустые состояния везде. Новый пользователь видит не пустые карточки,
      а объяснение: что здесь появится, почему сейчас пусто, что сделать.
- [ ] A2. Экран первого входа: короткий путь «подключите проект → запустите
      проверку → вот что вы увидите». Сейчас человек попадает в пустой обзор.
- [ ] A3. Объяснение каждого числа. У метрики — подсказка «откуда взято,
      за какой период, чему верить». Особенно там, где число может обмануть
      (посты > каналов, недельное окно вместо суточного).
- [ ] A4. Мобильная вёрстка. Владелец смотрит с телефона утром.
- [ ] A5. Директ и Метрика — явно опциональные. Сейчас неочевидно, что
      аналитик полноценно работает и без них (просто без данных о расходе).
      Клиента не должно отпугивать «надо настроить Яндекс.Директ».
- [ ] A6. Состояния загрузки: сейчас панели просто пустые, пока идут пять
      запросов. Скелетоны или индикатор.

### B. Клиентский путь (без этого продавать нельзя)

- [ ] B1. Аккаунты вместо одного пароля: регистрация, вход, свои проекты
      у каждого. Сейчас платформа однопользовательская.
- [ ] B2. Изоляция данных по владельцу на уровне запросов (не только UI).
- [ ] B3. Мастер подключения с готовым кодом: клиенту показывается сниппет
      endpoint'а `/api/internal/metrics` под его стек, который надо вставить
      к себе. Сейчас он должен догадаться сам по CONTRACT.md.
- [ ] B4. Переключатель проектов в шапке (сейчас «активный» один и меняется
      на отдельной вкладке).
- [ ] B5. Автоназвание типов событий для ленты (не только шагов воронки).
- [ ] B6. Страница-описание продукта для входящих (что это, кому, как работает).

### C. Надёжность

- [ ] C1. Тесты на платформу: `tests/test_platform_api.py` — авторизация,
      каждый endpoint, дедуп сигналов, извлечение воронки. Сейчас платформа
      не покрыта совсем, а ядро покрыто 463 тестами.
- [ ] C2. Починить `tests/test_daily_review.py`: три теста ищут файл по
      захардкоженному пути `/home/claude/growthagent-main` и падают везде,
      кроме одной машины.
- [ ] C3. Восстановить повреждённые загрузкой файлы: `PROJECT_STATE_GROWTHAGENT.md`
      (в нём лежит копия .env.example), корневые дубликаты `analyzer.py`,
      `main.py`, `db.py` и т.п. — они мешают и путают (uvicorn находил
      корневой main.py вместо app/main.py).
- [ ] C4. Аккуратные ошибки: любой отказ внешнего источника объясняется
      человеческим языком, а не «Ошибка 502».
- [ ] C5. Журнал действий владельца (кто что принял/отклонил и когда).

### D. Ценность аналитики

- [ ] D1. Список минус-фраз готовым блоком с кнопкой «скопировать» —
      по данным глубокой проверки Директа. Решение остаётся за человеком
      (автоматические правки рекламы сознательно НЕ делаем).
- [ ] D2. Сравнение периодов: неделя к неделе, до/после изменения.
- [ ] D3. Когорты по источнику: доходят ли до оплаты те, кто пришёл
      из разных каналов, по-разному.
- [ ] D4. Оценка «сколько данных нужно, чтобы вывод стал надёжным» —
      честный ответ на «когда уже будет понятно».
- [ ] D5. Чат аналитика умеет ссылаться на конкретные экраны и цифры.

### E. Коммерция (последнее по очереди, не по важности)

- [ ] E1. Тарифы и оплата (ЮKassa, как в АвтоПосте).
- [ ] E2. Ограничения бесплатного тарифа (сколько проектов, глубина истории).
- [ ] E3. Онбординг-письма/уведомления о важных событиях.

## Журнал работ

Каждая запись: дата, что сделано, что проверено, что осталось.
Новые записи — сверху.

<!-- ЖУРНАЛ: новые записи добавлять сразу под этой строкой -->

### 2026-07-26 — база платформы
Аналитик переведён из Telegram в веб: вход владельца, дашборд (KPI, воронка,
динамика, решение Growth Loop, сигналы), вкладки «Реклама», «Лента»,
«История», «Отчёты», «Чат», «Проекты». Мастер подключения любого проекта
(три поля + автообнаружение endpoints). LLM переведён на Yandex/DeepSeek.
Этапы воронки переименовываются, есть ИИ-автоназвание по сайту проекта.
Задачи уходят в разработку через GitHub Issue (@claude).
Проверено: 463 теста ядра проходят, скриншоты всех вкладок сняты.
